<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/auth_to_main"
    app:startDestination="@id/authenticationActivity">

    <activity
        android:id="@+id/authenticationActivity"
        android:name="com.example.appfall.views.activities.AuthenticationActivity"
        android:label="activity_authentication"
        tools:layout="@layout/activity_authentication" />
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.appfall.views.activities.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
</navigation>